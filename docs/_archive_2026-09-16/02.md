# 02 — Test coverage: real break loop actually reaches the final mix

**File**: new `src/test/real-break-mix.test.ts`.

**Change**: `src/test/break-loop.test.ts` covers the WAV decoder and the
loader in isolation (9 tests) but nothing asserts the real break loop
actually lands in `OfflineStubBackend.render()`'s output mix. Add an
integration test: find/force a seed that yields `patternFamily === 'amen'`
(see `scripts/render-real-break-demo.mjs`'s `findSeedForFamily` helper for
the pattern — reuse or extract it), render, and assert the drop-section
bars carry measurably more energy/a different spectral signature than the
same seed would with `buildRealBreakBus` returning `null` (you'll likely
need to export `buildRealBreakBus` from `OfflineStubBackend.ts`, currently
module-private, or add a way to disable it — see ticket 08, which adds a
real toggle this test could then drive instead of a private-export hack).

**Do not**: mock `loadBreakLoopMono` to fake success. Test against the
real committed assets in `src/assets/samples/breaks/` — that's the actual
integration surface, and it's small (two ~360KB WAVs), there's no reason
to mock it away.

**Done when**: a test fails if `buildRealBreakBus`'s call site in
`render()` is ever accidentally removed or short-circuited, which is
exactly the gap today (nothing would catch that regression).

**Verify**:
```bash
npx vitest run src/test/real-break-mix.test.ts
```
