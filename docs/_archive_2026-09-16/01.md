# 01 — Test coverage: dubstep ≠ half-time-drop is asserted, not just eyeballed

**File**: new `src/test/song-shape-divergence.test.ts` (or extend
`src/test/structure-shapes.test.ts`).

**Change**: Add an automated test that plans `structureEngine` across a
range of seeds (e.g. 1–50) for both `'dubstep'` and `'half-time-drop'`
songShapes and asserts they diverge — `bassRole.character` differs for a
meaningful fraction of seeds, and/or the energy-curve values at drop bars
differ deterministically (they should, `dropBoost`/`introDip` differ by
shape regardless of RNG). This bug was found and fixed twice this session
(see `docs/SESSION-DUMP.md`'s "Shared structure engine" workstream) and
verified only by manually re-running `scripts/render-styles.mjs` and
reading the printed output — there is currently no regression guard.

**Do not**: change the actual fix in `StructureEngine.ts` to make a test
pass. The RNG-decorrelation fix (`dubstepShape` consuming one throwaway
`rng()` draw) is already correct and confirmed by real rendering — this
ticket is purely about adding coverage so it can't silently regress.

**Done when**: the new test file exists, asserts real divergence across
multiple seeds (not one cherry-picked seed), and passes.

**Verify**:
```bash
npx vitest run src/test/song-shape-divergence.test.ts
npx tsc --noEmit
```
