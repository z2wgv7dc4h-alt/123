# PAID — epic-close v2 (tight, paperwork + verify only)

## Already done (do NOT re-implement)
Live master bus + kick→bass duck + duckEnvelopeStep + teardown + honesty selectBest fix are in tree from FCC.

## Do only (in order)
1. Mark GRUNT checkboxes done in `_cos-runs/EPIC-plan.md` (honest — match code).
2. Write `_cos-runs/EPIC-listen-slap-DONE.md` (≤40 lines): landed / listen caveat / non-goals left.
3. `npm.cmd test -- --run` then `npx.cmd tsc --noEmit`. If red: fix **only** failing tests/compile — no new features.
4. Stop. Soft-pass forbidden.

## Out
Critic subagent deep pass, UI/CSS, LayersChips, audio P1/P2, FCC, rewrites of PreviewPlayer unless a test forces a 1-line fix.
