# S-2 Caption: concrete DnB production language

**Status: Done** `ab4ba96`, `f8d943c`, `e173211`, `c9c878f` (short genre-aware caption, no BPM, no default guitar).

File: `src/core/prompt/buildAceCaption.ts`, `src/core/types/index.ts` (`DEFAULT_DESCRIPTORS`), `src/ui/hooks/useStudioStore.ts:1090-1095`, `src/test/ace-caption-vary.test.ts`

Change: With default knobs and no layers, the caption must contain concrete
production terms: `174 bpm`, `reese bass` (detuned/moving), `snare on 2 and 4`
(half-time: `snare on 3`) and `amen break chops`. Remove `distorted guitar riffs`
and `energetic dancefloor` from the default text; guitar words only when
`layers.guitar`. `half-time-drop` must not get `dubstep` words; only
`songShape === 'dubstep'` does.

Do not: touch `"thinking"` in the bridge. Do not change steps, guidance, shift,
DCW, cover or the checkpoint. Do not add artist names. Do not render.

Done when: string tests assert the concrete terms are present by default;
`distorted guitar` is absent unless `layers.guitar`; the half-time-drop caption
has no `dubstep`; existing energy-band regexes are updated deliberately, not
deleted; the caption stays within ACE's 5–12 keyword-phrase guidance.

Verify: `npx vitest run src/test/ace-caption-vary.test.ts && npx tsc --noEmit`
