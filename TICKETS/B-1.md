# B-1 Remove the 174 BPM lock

**Status: Done `1b3a702`.**

Tempo is a store setting (70–200, `clampProductBpm`). Generate sends the user's
tempo. Sketch honors it; real break loops switch off >6 BPM from 174. The
ParamPanel shows a tempo input instead of a "174 BPM" pill.
