# 05 — Studio: `extract` task type for real separated ACE stems

**Status: Open.** Extract is base-model only; needs base in a second ACE slot (`ACESTEP_CONFIG_PATH2`).

**File**: `sidecar/ace_bridge_server.py`, `src/core/backends/AceStepBackend.ts`.

**Change**: Right now every "stem" `AceStepBackend.render()` returns
mirrors the same mix blob (`stemsShareMixBlob` in `AceStepBackend.ts`,
honestly labeled as such in warnings/notes). The real fix, per
`docs/HANDOFF.md` §3 item 2 (corrected there from an earlier wrong "lego"
framing — read that correction, don't rediscover it): ACE-Step's real
`task_type: "extract"` isolates one named instrument track from audio
that already exists. After a Studio mix renders, issue follow-up
`/release_task` calls with `task_type: "extract"`, the just-rendered mix
as the `src_audio` multipart file, and one call per `TRACK_NAME` wanted
(kick/bass/drums at minimum — see `acestep/constants.py`'s `TRACK_NAMES`
in the local ACE-Step-1.5 repo for the full list). This reuses the same
multipart upload plumbing as ticket 04 — do 04 first, this is a natural
follow-on.

**Do not**: remove the existing "stems mirror mix" warning until every
stem actually being returned is confirmed as a real, distinct extract.
Partial rollout (e.g. only kick/bass wired, not hats/perc) must keep the
honest warning for whichever stems aren't real yet — don't let the
warning go stale/wrong.

**Done when**: at least the kick and bass stems returned by
`AceStepBackend.render()` are verifiably distinct audio, not
byte-identical to the mix or to each other.

**Verify**:
```bash
scripts/windows/start-ace-stack.ps1
npx vitest run src/test/ace-backend-render.test.ts
```
Add an assertion comparing returned stem blob bytes for inequality
(kick ≠ bass ≠ mix), plus a real listen to confirm the isolated track
actually sounds like that instrument.
