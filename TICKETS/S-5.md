# S-5 Style-ref cover strength 0.35 vs 0.7

File: `src/core/backends/AceStepBackend.ts` (`ACE_COVER_STRENGTH`), `sidecar/ace_bridge_server.py:413`, `src/test/ace-backend-render.test.ts`

Change: Add an optional per-job cover-strength override (default
`ACE_COVER_STRENGTH` = 0.25). In the bridge, replace `float(req.get("audioCoverStrength") or 0.25)`
with a None-check so explicit values survive. Payload tests for 0.35 and 0.7.

Do not: change the default until the user listens. Do not render. Do not
touch `thinking`, steps or the caption. Do not send the style-ref file without
`ownerAttested`.

Done when: payload tests show `audioCoverStrength` 0.35 and 0.7 reach the
request body, together with `srcAudioBase64`, only when an attested style ref
exists. The user later runs the first live cover at both values and reports.

Verify: `npx vitest run src/test/ace-backend-render.test.ts && npx tsc --noEmit`; listening by the user only
