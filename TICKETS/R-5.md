# R-5 Mastering stage (loudness)

**Status: Open.**

ACE only peak-normalizes to -1 dB. Add an optional master chain on the Studio
take (glue compression + limiter toward about -9 LUFS integrated), applied on
export and optionally on preview. Report measured loudness and peak honestly
(sample-peak unless real true-peak is implemented; see `StemFile` peak notes).

Done when: measured integrated loudness and peak on a fixture match the target
within tolerance, and the chain can be turned off.
