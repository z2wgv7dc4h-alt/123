# S-7 Real stems via Demucs v4 (bridge /stems)

**Status: Done** `78f39d5`.

File: `sidecar/ace_bridge_server.py`, `scripts/windows/start-ace-stack.ps1`
(comment only), `src/core/backends/AceStepBackend.ts`,
`src/core/types/index.ts`, `src/ui/hooks/useStudioStore.ts`,
`src/ui/components/StemMixer.tsx`, `src/test/demucs-stems.test.ts`,
`sidecar/test_ace_bridge_payload.py`.

Change: ACE Step only returns one mix, so every "stem" mirrored that mix and
Mute/Solo was preview-relative. `POST /stems {mixWavBase64}` now runs Demucs v4
`htdemucs` (MIT; GPU when torch reports CUDA, else CPU) on a temp WAV and returns
`{stems: [{id, wavBase64, durationSec}]}` for drums/bass/other/vocals, or 501
with the `pip install demucs` hint when Demucs is absent (never auto-installed).
`AceStepBackend.separateStems(mixBlob)` maps the four tracks to StemFiles
(drums/bass/other). The store's `separateStems()` swaps the mirrored lanes,
sets `RenderResult.stemsReal`, re-arms the live mixer, and removes the
"stems mirror mix" warning only once real stems exist. UI: **Split stems
(Demucs)** next to the mixer in More, plus a "Real stems" pill.

Done when: bridge returns 501 when the Demucs import fails (mocked); the store
replaces the mirrored stems with real drums/bass/other and marks them real
without a render; `npx tsc --noEmit` plus the named tests pass.

Verify:
```bash
python -m unittest sidecar/test_ace_bridge_payload.py
npx vitest run src/test/demucs-stems.test.ts
npx tsc --noEmit
```
