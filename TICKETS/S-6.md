# S-6 Style-ref cover strength default 0.25 -> 0.55

**Status: Done** `493084c`.

File: `src/core/backends/AceStepBackend.ts` (`ACE_COVER_STRENGTH`, line 89),
`sidecar/ace_bridge_server.py:467-470`, status/log line at
`sidecar/ace_bridge_server.py:599`, `src/test/ace-backend-render.test.ts`

Change: `ACE_COVER_STRENGTH` default becomes 0.55 (valid override range stays
0.35-0.7 — clamp/validate, don't just move the default). This only applies
when a style-ref file is attached (`task_type=cover`); no attached file means
no `audio_cover_strength` in the payload at all, same as today. Extend the
existing status/log line at `ace_bridge_server.py:599` (which already prints
`use_adg`/`thinking`) to also print `task_type` and `audio_cover_strength`
whenever `task_type=cover`, so the strength actually in effect is visible per
job.

Do not: redesign any UI. Do not touch `thinking` for the cover path — it must
stay `False` (line 470); ACE skips the LM on cover regardless, so this is a
no-op setting, not a behavior change. Do not render. Do not send the
style-ref file without `ownerAttested`.

Done when: default `ACE_COVER_STRENGTH` is 0.55; an explicit override outside
0.35-0.7 is rejected/clamped (add the case to the test file); the status
output shows `task_type=cover` and the strength value used, for any job that
attaches a style-ref file; `thinking` is confirmed still `False` on the cover
path.

Verify: `npx vitest run src/test/ace-backend-render.test.ts && npx tsc --noEmit`
