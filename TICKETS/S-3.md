# S-3 Same seed, 50 vs 64 inference steps — user listens

**Status: Obsolete.** Studio uses turbo (8 steps). See docs/ACE-NOTES.md.

File: `src/core/backends/AceStepBackend.ts` (`ACE_INFERENCE_STEPS`), `src/core/types/index.ts` (`RenderJob`), `src/test/ace-backend-render.test.ts`

Change: Add an optional per-job steps override (default `ACE_INFERENCE_STEPS`
= 50) so the user can render the same seed at 50 and 64 without editing code.
Fix the misquoted comment: ACE `INFERENCE.md:366` says base is "recommended
32-64", and `:1015` says 64+ for quality.

Do not: change the default to 64 until the user reports back. Do not run a
render, and do not sit in a generate loop. Do not change any other ACE parameter.

Done when: a payload test shows `inferenceSteps: 50` by default and `64` when the
override is set, and the seed is identical in both payloads. The user then renders
both and reports which one wins.

Verify: `npx vitest run src/test/ace-backend-render.test.ts && npx tsc --noEmit`; listening by the user only
