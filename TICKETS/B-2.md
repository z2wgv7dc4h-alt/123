# B-2 Genres

**Status: Done `c9c878f`.**

`GenreId` = dnb | dubstep | halftime | jungle (`GENRES` in `src/core/types`).
Genre-aware captions (`GENRE_CAPTIONS` in `buildAceCaption.ts`), presets carry a
genre and tempo, genre row in `SimpleWant`, "Dubstep feel" shape retired from the
picker (the id stays in the engine for old drafts), `acePayload.captionFamily`
reports Dubstep / Jungle / DnB.
