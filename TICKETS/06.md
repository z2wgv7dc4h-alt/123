# 06 — Studio: enable DCW (free quality correction)

**File**: `src/core/backends/AceStepBackend.ts`, `sidecar/ace_bridge_server.py`.

**Change**: DCW (Differential Correction in Wavelet domain) is a
training-free, negligible-compute, sampler-side quality correction —
read `C:\Users\RIGGUSPIG\Documents\ACE-Step-1.5\docs\en\DCW.md` (local,
real, authoritative) before touching this. Per that doc: `dcw_enabled` is
**enabled by default for Turbo models but disabled by default for
non-Turbo** — this project always uses `acestep-v15-base` (non-Turbo), so
DCW is currently silently off on every single render. Wire
`dcwEnabled: true` and `dcwMode: "low"` (the doc's own "sensible starting
point, not canonical" recommendation) into the render request as real,
named parameters — not hardcoded silently, so they can be toggled/A-B'd
later.

**Do not**: touch `dcw_scaler`/`dcw_high_scaler` defaults (0.05/0.02) or
try other `dcwMode` values without real A/B evidence. This ticket is
scoped to flipping `dcw_enabled` on with the doc's own recommended
starting mode — nothing more, per this whole project's "get a source,
don't guess" rule.

**Done when**: the flag is wired end to end (bridge accepts it, forwards
it to the real ACE API) and a real A/B render (same seed, DCW on vs off)
has actually been generated and compared.

**Verify**:
```bash
scripts/windows/start-ace-stack.ps1
npx vitest run src/test/ace-backend-render.test.ts
```
Then: two renders, same seed, DCW on vs off, sent to the user for a real
listen. No claim of improvement without that — this project's established
rule all session.
