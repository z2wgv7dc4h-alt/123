# R-2 Keep take + Redo / Extend on the selected section + take history

**Status: Open.** Next ticket. The lead writes the paste after reading
`SectionTimeline.tsx`, `Waveform.tsx` and the store's section actions.

Goal:
- "Keep take" stores the current Studio mix blob as take v1 (in memory; no disk).
- The selected section gets **Redo** (repaint that bar range in seconds from the
  structure grid, caption = current caption) and **Extend +8 / +16** (last
  section: repaint past the end; other sections wait for R-4).
- Each edit result becomes take vN+1; **Undo** goes back without rendering.
- Replaces the current Expand / Repeat / ×2 buttons, which re-render the whole song.
- Studio only; on Sketch the buttons are disabled with "Studio only".

Done when:
- Tests: Redo sends `edit.repaint` with the section's seconds and the take blob;
  Extend on the last section sends `endSec` > take length; Undo restores the
  previous blob without calling render; Sketch disables the buttons.
- `npx.cmd tsc --noEmit` plus the named tests pass.
