# S-4 Download acestep-v15-sft (not on disk)

**Status: Obsolete.** ACE README rates turbo above SFT; do not download SFT for quality.

File: `docs/REVIEW-SOUND.md` (record result), `src/core/backends/AceStepBackend.ts:283` (`checkpointId`, later), `sidecar/ace_bridge_server.py:399` (`model`, later)

Change: Checked 2026-09-16: `C:\Users\RIGGUSPIG\Documents\ACE-Step-1.5\checkpoints`
has `acestep-v15-base` and `acestep-v15-turbo`, and **no `acestep-v15-sft`**.
Decided (user, 2026-09-16): no download for now; this stays a ticket.
Step 1 (user, when they choose): download the SFT DiT weights with ACE-Step's own downloader.
Step 2 (agent, after the user confirms the folder exists): make `checkpointId`
a named constant and select `acestep-v15-sft`, with a payload test.

Do not: download anything as an agent. Do not change `checkpointId` before the
folder exists. Do not touch LoRA. Do not change steps or caption in the same change.

Done when: the SFT folder exists on disk (user-confirmed); a payload test asserts
`checkpointId: 'acestep-v15-sft'`; after S-1, the heard label shows sft.

Verify: `npx vitest run src/test/ace-backend-render.test.ts && npx tsc --noEmit`
