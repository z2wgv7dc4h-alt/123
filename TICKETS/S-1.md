# S-1 Show which backend + checkpoint was actually heard

**Status: Done** `ab4ba96` (heard-backend chip; model list from probe).

File: `src/App.tsx` (honesty chip), `src/ui/components/PowerExtras.tsx` (`STUDIO_MODELS`), `sidecar/ace_bridge_server.py` (`:399`, `:452`), new `src/test/heard-backend-label.test.ts`

Change: Add a "heard" label derived only from `result.backendId` +
`result.checkpointId` (e.g. `Sketch · CPU` / `Studio · acestep-v15-base`). Keep
the pre-Generate chip as "will use" from the probe. The bridge must echo the
`model` it sent in `checkpointId` instead of `ACESTEP_CONFIG_PATH`. Fix
`STUDIO_MODELS` so base is shown as active, not turbo. Probe check: when
`GET /ace-bridge/probe` answers, show its `checkpoint` field.

Do not: render audio. Do not change which backend `generate()` chooses. Do not
change the checkpoint.

Done when: a test with a fake `result` from each backend gets the right heard
label; a test with a mismatched `productTier` shows that the label follows
`result`, not tier; `STUDIO_MODELS` no longer claims turbo is active; the bridge
response test/inspection shows `checkpointId == payload["model"]`.

Verify: `npx vitest run src/test/heard-backend-label.test.ts && npx tsc --noEmit`
