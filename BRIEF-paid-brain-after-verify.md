# PAID brain — next product after verify gate

## Do
1. Run npx.cmd tsc --noEmit + npm.cmd test -- --run; record real EXIT into _cos-runs/PAID-VERIFY-LIVE.md (no invented counts). Soft-pass forbidden.
2. From PAID-VERIFY-P1 + north star: pick highest open PRODUCT gap not owned by FCC ZIP CRC (e.g. seed→audio repro brief for next FCC queue, or slap/listen Critic P0 still open). Write ordered next FCC briefs with hard-disjoint files.
3. No src/ edits this session unless verify finds a 1-line blocker you own and FCC is not on that file.

## Out
HelpTips micro-briefs; dual-write OfflineStub/PreviewPlayer/zip.ts with FCC.
