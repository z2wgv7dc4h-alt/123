@AGENTS.md

Delegate research to subagents so main context stays clean.
`docs/HANDOFF.md` is the pickup entry point every session.
Archived, non-binding docs live in `docs/_archive_2026-09-16/` — do not treat them as law.
