# Synthetic QA fixtures (always safe)

Deterministic, non-copyrighted audio for analyzer / provenance tests.

| File | Source |
|------|--------|
| `qa_style_ref_synthetic.wav` | Copy of `exports/qa_style_ref_synthetic.wav` (mono PCM, ~174 BPM click-style QA) |

Do not place user/catalog material here — use `../license-clean/` for that.
