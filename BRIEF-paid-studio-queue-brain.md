# PAID brain — Studio product queue from Researcher handoff (2026-09-14)

Soft-pass forbidden. No `src/` edits unless a 1-line honesty bug is blocking the next FCC GRUNT and FCC is not on that file.

## Context
Researcher full handoff to CoS. ACE 5080 pins = FCC parallel (scripts/docs only). You plan Studio code queue.

Wyatt locked priorities:
1. Listen/modify: section→expand; map-synced player; expand+gen same seed; Vary=new idea
2. Live mute/solo/gain — Channels GREEN; do not re-block
3. Process Simple order: Style/prompt → Shape → Gen → Hear → Expand → Layers → Export
4. Dubstep + trap fat-beats + dancefloor slap
5. Guitar/solo generative (legal lego only) — AFTER ACE patch pin
6. ACE prove on 5080 (probe hasGpu + real WAV Play)

## Ordered PRODUCT queue (write `_cos-runs/STUDIO-QUEUE.md`)
Hard-disjoint FCC briefs with named files for CoS to launch next:
A. **Snare-on-3** — fix any remaining snare-on-2 for 174 half-time / trap-bounce (store once said snare on 2).
B. **Trap-bounce** Shape/layer flavor: 808 + triplet hats + snare-on-3 at 174 — not a trap-DAW fork.
C. **Drop silence→impact** anatomy (withhold → riser/roll → near-silence → kick+snare+sub land).
D. **Subgenre Shape→family+caption map** (neuro/liquid/jump-up/techstep/rock-DnB vibe / 174 half-time dub).
E. **ACE prove brief** (after pins land): cu128, base not xl/turbo, bridge steps≈50 guidance≈7 shift=3.0, hasGpu + real WAV.
F. **Lego/guitar** only after ACE #809 silence patch pin — track_name=guitar; never claim true stem isolation until extract proven.

## Must NOT
Artist clones, catalog/YT rips, non-owned LoRA, fake live/fake ACE/fake isolated stems, AGPL Matchering/Pedalboard, ACE Retake wiring (Expand/Vary stay structure/seed).

## Verify
If approval surface works: `npx.cmd tsc --noEmit` + `npm.cmd test -- --run` → `_cos-runs/PAID-VERIFY-LIVE.md` with real EXIT. If denied again: say so honestly; do not invent counts. EXIT=0 only if verify green OR brief scope was docs-only with honest blocker recorded.
