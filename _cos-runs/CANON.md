# DnB Studio CANON (Wyatt 2026-09-14) — in stone

## Product
1. **Primary:** ACE Generate→Play on RTX 5080 when hasGpu (`selectBest`→`ace-step`). Studio-quality DnB Wyatt already heard.
2. **ACE:** acestep-v15-base; thinking; steps≈50; guidance≈7; shift≈3; cu128; QUANTIZATION/COMPILE/FLASH_ATTENTION=false; LM_BACKEND=pt. Healthy `:8766`+`:8001` → Studio path only — never reinstall thrash.
3. **Stub/shapes/HelpTips:** secondary. Slip if they replace ACE quality.
4. **Listen-first:** click section synced to playhead; Expand→Generate = same seed; Vary = new idea; one shared clock.
5. **Live Channels:** mute/solo/gain mid-play (Tone.Channel). Params = next Generate.
6. **ACE stems:** share the mix until lego — never claim isolated stems. Sketch = real stems.
7. **Sound:** DnB + dubstep snare-on-3 + trap-bounce @174. Slap, not toy. No artist clones.
8. Wyatt’s label wins.

## Ops
1. `scripts\cos.ps1` status|run|stop only.
2. FCC model: `anthropic/nvidia_nim/nvidia/nemotron-3-super-120b-a12b` only.
3. FCC `:8082`+freecc; paid clears proxy. Names DnB-FCC / DnB-Paid.
4. Never both idle. Pre-stage next BRIEF. FCC never idle.
5. 429 → park paid (zero relaunch). 2-strike thrash. Grep before relaunch. No double-hand.
6. Verify in BRIEF — never Grok npm/tsc. No Critic acks. Wyatt: one-line DONE/FAIL.
7. Eyes: board `:8787` + COS-STATUS + TASK-LOG. Read `_cos-runs/ANTI-THRASH.md` every wake.
8. This CANON overrides all stale HANDOFF lore.
