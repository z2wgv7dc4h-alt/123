# FCC — ONE SHOT ACE path (thin). Soft-pass forbidden. Max 8 tool rounds mental budget.

DO ONLY:
1. GET http://127.0.0.1:8766/health — confirm 5080 + base + upstreamUp.
2. If Studio/CLI can kick one ACE generate with steps=50 guidance=7 shift=3 thinking — do ONE wait; else document exact command/API to run and stop.
3. Write _cos-runs/FCC-ACE-ONE-SHOT-RESULT.md (pass/fail honest). STOP.

OUT: no HelpTips, no stub digs, no refactor tours, no polling loops.
