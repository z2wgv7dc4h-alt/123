# PAID brain — close EPIC-listen-slap

## Context
FCC already landed live master bus + kick→bass duck + duckEnvelopeStep + teardown in `PreviewPlayer.ts` / `ducking.ts`. Honesty selectBest test fixed. Suite was green except env honesty (now fixed).

## Your job (paid / hard)
1. Read `_cos-runs/EPIC-plan.md` + the live graph code. Update plan checkboxes to match reality.
2. Critic-style noob journey pass focused on live-tweak punch parity (open→style→generate→play→tweak→rehear→export). Fix only concrete cited gaps — no scope expansion.
3. Ensure `npm.cmd test -- --run` + `npx.cmd tsc --noEmit` fully green with ACE sidecar possibly up.
4. Write `_cos-runs/EPIC-listen-slap-DONE.md` one-pager: what landed, listen caveat, remaining non-goals.

## Out of scope
Sketch LayersChips, UI/CSS rewrites, P1/P2 from critic-audio cycle-4.

## Done
Tests + tsc green; plan checkboxes honest; DONE.md written.
