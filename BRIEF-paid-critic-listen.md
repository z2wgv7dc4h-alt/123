# PAID — docs-only Critic listen pass (NO src edits)

## Do
1. Adversarial noob journey read-only: open→style→generate→play→tweak→rehear→export. Cite real files.
2. Write ONLY `_cos-runs/CRITIC-LISTEN-PASS.md`: table-stakes gaps + FCC GRUNT handoff list with **disjoint file paths** (not OfflineStub/export/slap while FCC owns those).
3. Soft-pass forbidden. No src/ edits. No npm/tsc required if no src touch.

## Out
Implementing fixes yourself; dual-write with FCC; ops archaeology.
